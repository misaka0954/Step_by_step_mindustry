plugins {
    id 'java'
}

group 'uwu.misaka'
version '0.0-HENTAI'

repositories {
    mavenCentral()
}

dependencies {

}

test {
    useJUnitPlatform()
}
task dist(type: Jar) { manifest { attributes 'Main-Class': "uwu.misaka.stepbystepmindustry.Ichi" } from { configurations.compileClasspath.collect { it.isDirectory() ? it : it.name.endsWith("pom") ? it : zipTree(it) } } from files(sourceSets.main.output) }